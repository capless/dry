import React from "react";
import LinearProgress from "components/linear-progress";

export default {
  title: "Library|LinearProgress/Default",
};


export const primary = () => <LinearProgress />;

export const secondary = () => <LinearProgress color="secondary" />;
